export interface IProduct{

    id: string;
    name: string;
    locality: string;
    city: string;
    thumb: string;
    aggregate_rating: number;
    rating_text: string;
    min_price: number;
}
